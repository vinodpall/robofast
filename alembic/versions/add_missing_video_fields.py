"""add_missing_video_fields

Revision ID: add_missing_video_fields
Revises: 42296d435463
Create Date: 2024-04-29 13:00:00.000000

"""
from alembic import op
import sqlalchemy as sa
from sqlalchemy import inspect


# revision identifiers, used by Alembic.
revision = 'add_missing_video_fields'
down_revision = '42296d435463'
branch_labels = None
depends_on = None


def upgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    inspector = inspect(op.get_bind())
    columns = [col['name'] for col in inspector.get_columns('videos')]
    
    # 添加缺失的字段
    if 'description' not in columns:
        op.add_column('videos', sa.Column('description', sa.Text(), nullable=True))
    
    if 'type' not in columns:
        op.add_column('videos', sa.Column('type', sa.String(length=20), nullable=True))
    
    if 'carousel_add_time' not in columns:
        op.add_column('videos', sa.Column('carousel_add_time', sa.String(length=50), nullable=True))
    
    # 删除不需要的字段
    if 'video_url' in columns:
        op.drop_column('videos', 'video_url')
    
    if 'image_url' in columns:
        op.drop_column('videos', 'image_url')
    # ### end Alembic commands ###


def downgrade() -> None:
    # ### commands auto generated by Alembic - please adjust! ###
    inspector = inspect(op.get_bind())
    columns = [col['name'] for col in inspector.get_columns('videos')]
    
    # 恢复删除的字段
    if 'video_url' not in columns:
        op.add_column('videos', sa.Column('video_url', sa.String(length=255), nullable=True))
    
    if 'image_url' not in columns:
        op.add_column('videos', sa.Column('image_url', sa.String(length=255), nullable=True))
    
    # 删除新添加的字段
    if 'description' in columns:
        op.drop_column('videos', 'description')
    
    if 'type' in columns:
        op.drop_column('videos', 'type')
    
    if 'carousel_add_time' in columns:
        op.drop_column('videos', 'carousel_add_time')
    # ### end Alembic commands ### 